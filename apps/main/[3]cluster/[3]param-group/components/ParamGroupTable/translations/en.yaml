columns:
  actions: Actions
actions:
  apply: Apply
  copy: Copy
  edit: Edit
  delete: Delete
apply:
  loading: Applying the parameter group to the cluster {{ msg }}...
  success: Successfully apply the parameter group
  fail: 'Failed: {{ msg }}'
copy:
  loading: Copying...
  success: Successfully create a new parameter group
  fail: 'Failed: {{ msg }}'
edit:
  disabled: Only user-definded parameter groups are editable
delete:
  disabled: Only user-definded parameter groups are deletable
  confirm: Are you sure to delete the {{ name }} parameter group?
  loading: Deleting...
  name: Delete Paramater Group
  success: The paramater group is deleted
  failed: Failed to delete the paramater group
