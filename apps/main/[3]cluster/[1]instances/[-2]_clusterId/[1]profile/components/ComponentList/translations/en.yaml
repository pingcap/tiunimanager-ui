title: Components
list:
  title: '{{ name }} List'

usage:
  mem: MEM
  storage: STO
columns:
  actions: Actions
actions:
  scaleIn: Delete
scaleIn:
  confirm: Are you sure to delete the {{ type }} instance on {{ ip }}?
  name: Delete