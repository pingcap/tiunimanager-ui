title: Components
list:
  title: '{{ name }} List'

usage:
  mem: MEM
  storage: STO
columns:
  actions: Actions
actions:
  scaleIn: Scale In
scaleIn:
  confirm: Are you sure to scale in the {{ type }} instance on {{ ip }}?
  name: Scale In